# Let Nixpacks auto-detect Python from requirements.txt
# Only specify additional packages needed for scipy/numpy compilation

[phases.setup]
nixPkgs = ["gcc", "gfortran", "pkg-config"]

[start]
cmd = "python manage.py migrate && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
