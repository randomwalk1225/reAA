databases:
  - name: discharge-db
    databaseName: discharge
    user: discharge
    plan: free

services:
  - type: web
    name: discharge-measurement
    runtime: python
    plan: free
    rootDir: webapp
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn config.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: false
      - key: ALLOWED_HOSTS
        value: .onrender.com
      - key: DATABASE_URL
        fromDatabase:
          name: discharge-db
          property: connectionString
      - key: HRFCO_API_KEY
        value: 9E50673B-2D96-4436-BA86-756E81D3C738
